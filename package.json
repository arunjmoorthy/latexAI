{
  "name": "latex-copilot",
  "version": "1.0.0",
  "description": "An AI-powered LaTeX editor with real-time suggestions and PDF compilation",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"cd latex-editor && npm start\" \"cd latex-editor-backend && npm start\"",
    "install-all": "npm install && cd latex-editor && npm install && cd ../latex-editor-backend && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  },
  "author": "",
  "license": "MIT"
}
